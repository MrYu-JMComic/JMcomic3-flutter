org.gradle.jvmargs=-Xmx4096M -Dfile.encoding=UTF-8
# Keep the JDK selection in the local build environment (JAVA_HOME/Flutter
# configuration). Do not pin a developer-specific absolute path here.
android.useAndroidX=true
android.enableJetifier=true
# Canonical NDK version shared with the Rust cargo-ndk release workflows.
rust.ndkVersion=27.0.12077973
